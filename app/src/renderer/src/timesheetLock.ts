// 配る版ではタイムシートを開けないようにする（2026-08-30 の指示）。まだ「表を出してよいか」
// の判断が動いている段階なので、打ち込んでもらう前に落ち着かせる。開発版（npm run dev）
// では従来どおり使える——ロックしたまま開発はできない。
//
// **2026-08-31：打ち込み済みのものは消した**（db-schema.ts の timesheets を参照）。
// 表が並べるコマ番号は、抜けた区間の枚数を推定したまま組み立てている
// （docs/FRAME-GAPS.md 3 章は未解決）。枚数が 1 違えば下の打鍵は全部別のコマを指すが、
// **画面からは正しく見える。** 残しておくと次に開いたときに「前に打ったもの」として通る。
//
// **判定はここ 1 か所に置く。** 表を出す側（useTimesheet）とショートカット一覧の両方が
// 見るので、それぞれに書くと片方だけ配る版に残り、効かないキーの説明が出続ける。
export const TIMESHEET_LOCKED = !import.meta.env.DEV
