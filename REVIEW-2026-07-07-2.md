# コードレビュー指摘事項 第2回（2026-07-07 / v1.0.4 全体レビュー）

Git 差分ではなくコードベース全体（app main/preload/renderer + extension）を対象とした2回目のレビュー。
前回（REVIEW-2026-07-07.md、対応済み）と重複する指摘はない。
別セッション（他モデル）が本ファイルだけを読んで修正できるよう、根拠・再現条件・修正方針を自己完結で記載する。

**修正後の検証**: `cd app && npm run verify`（typecheck + vitest 231件）。個別の確認手順は各項に記載。

---

## 1.【バグ・優先度高】ローカルインポートした古い画像がグリッド先頭に楽観挿入され、並び順が壊れる（重複表示の可能性もある）

**場所**:
- `app/src/renderer/src/hooks/useCaptureSync.ts` の `onCapture` ハンドラ（`prependToGrid` 分岐）
- 発生源: `app/src/main/ipc-import.ts` の `importFiles`（`captured_at: Math.floor(srcStat.mtimeMs)` — 元ファイルの更新日時を撮影日時にする仕様）と、`captured-media.ts` が import 経路でも `capture:done` を broadcast すること

**問題**: `useCaptureSync` は「フィルタなし・date_desc」のとき `store.prependToGrid(img)` で新着をグリッド先頭に楽観挿入する。この経路はキャプチャ（`captured_at = Date.now()`）を想定しているが、フォルダドロップ/ファイルドロップのインポートも同じ `capture:done` を受けるため、**mtime が古い画像（例: 去年保存した画像）が「新しい順」グリッドの最上部に挿入される**。正しい位置に並ぶのはフィルタ変更や再起動でグリッドを再取得したときだけ。

さらに、グリッドはカーソルページング（`before = 最後に読み込んだ行の captured_at/id`）なので、先頭に挿入された古い画像の `captured_at` までスクロールが進むと **同じ行がページ取得でもう一度返ってきて `gridImages` に同一 id が2件並ぶ**（React の duplicate key 警告＋二重表示）。

**再現**: フィルタなし・新しい順で、更新日時が古い画像ファイルをウィンドウへドロップ → グリッド最上部に出る → そのままその画像の本来の位置までスクロールすると二重に表示される。

**修正方針**: `useCaptureSync.ts` の prepend 判定に「先頭挿入して正しい位置になるか」を追加する。先頭の既存行より新しいときだけ prepend し、それ以外は既存の `needsGridReload = true` 経路（デバウンス済み再取得）へ倒す:

```ts
const { gridImages } = useImageStore.getState()
const top = gridImages[0]
if (!filtered && sortOrder === 'date_desc' && (!top || img.captured_at >= top.captured_at)) {
  store.prependToGrid(img)
} else {
  needsGridReload = true
}
```

（クリップボード貼り付けは `captured_at = Date.now()` なので従来どおり prepend 側に入る。`img.source === 'import'` での分岐は貼り付けまで reload に倒してしまうため上記の時刻比較の方が良い）

---

## 2.【機能欠落・中】共有エクスポートの settings.json（スマートフォルダ）が、インポートで一切読み込まれない

**場所**: `app/src/main/ipc-share.ts`

**事実関係**:
- `shareExport` は `metadata.jsonl` に加えて `settings.json`（`{ version: 1, smartFolders }`）を書き出す（97〜101行目）
- ファイル冒頭コメントは「一括共有（… / **スマートフォルダ設定の取り込み**）の IPC ハンドラ」と書かれている
- しかし `shareImport` は `metadata.jsonl` しか読まず、`settings.json` を読むコードはコードベースのどこにも存在しない（grep 確認済み）
- `SettingsModal.tsx` の書き出し説明文も「キャプチャ、タグ、メモ、**スマートフォルダ**をフォルダへ保存します」と謳っている

つまり書き出された smartFolders は永遠に使い道がない。

**修正方針（どちらか。a を推奨）**:
- a. `shareImport` で `settings.json` を読み、`smartFolders` を検証（`settings.ts` の `smartFolders()` 正規化を再利用）した上で既存設定にマージする。`id` は衝突しうるので取り込み時に採番し直し（`Date.now()+index` 等）、`name` が完全一致する既存フォルダはスキップ。取り込んだ件数を戻り値に足して renderer 側のステータス表示に出す。renderer 側は `App.tsx` の `onShareImport` 完了後に `settings` の再取得（`useSettingsStore` へ反映）が必要
- b. 実装しないなら `shareExport` から settings.json 出力を削除し、SettingsModal の説明文とファイル冒頭コメントから「スマートフォルダ」を落とす

---

## 3.【バグ・中】Ctrl+A 全選択 → 削除 → Undo（または一部失敗）で、未ロード分の件数・一覧が復元されない

**場所**: `app/src/renderer/src/stores/imageStore.ts` の `removeImages` / `restoreImages`、`app/src/renderer/src/hooks/useSelection.ts` の `queueDelete` / `undoPendingDelete` / `commitPendingDelete`

**問題**: Ctrl+A は `listAllImages` でフィルタ一致全件（最大5000）の id を選択するため、**グリッドに未ロードの画像 id も選択に含まれる**。削除時:

- `removeImages(ids)` は `gridTotalCount` を `ids.size` ぶん減らす（未ロード分も含む。これは正しい）
- ただし snapshot に積まれるのは「現在 `gridImages` / `timelineImages` にロード済みの行」だけ
- `restoreImages` は件数の戻し量を **snapshot に入っていた行の数（`restoredCount`）** で計算する

その結果、グリッドを1ページ（50件）しか読み込んでいない状態で 500 件を Ctrl+A → Delete → Ctrl+Z（Undo）すると、件数は −500 された後 +50 しか戻らず、**サイドバーの「◯ 枚」表示が 450 少ないまま**になる（DB は削除されていないので、フィルタ変更や再起動で直る）。タイムラインを一度も開いていないセッションで顕在化する（開いていれば timeline snapshot が全件持っているため偶然合う）。

同様に `commitPendingDelete` の失敗経路 `restoreImages(snapshot, failedIds)` でも、未ロードの画像の削除が失敗した場合に件数が戻らない。

**修正方針**: `RemovedImagesSnapshot` に削除対象の id 全体を持たせ、件数復元はリスト復元と切り離してそこから数える:

```ts
export type RemovedImagesSnapshot = {
  grid: { index: number; image: ImageRow }[]
  timeline: { index: number; image: ImageRow }[]
  gridTotalCount: number | null
  removedIds: number[]   // 追加: removeImages に渡された ids 全体
}
// restoreImages 内:
const restoredCount = snapshot.removedIds.filter(shouldRestore).length
```

リスト（配列）の復元自体は従来どおり snapshot にある行だけでよい（未ロード分はスクロール時のページ取得で自然に出てくる）。`imageStore.test.ts` に「snapshot 外 id を含む復元で件数が戻る」ケースを追加推奨。

---

## 4.【バグ・中】フォルダ取り込みの200件上限に「画像以外のファイル」もカウントされ、画像が取り込まれないことがある

**場所**: `app/src/main/ipc-import.ts` の `collectImportFiles`（37行目付近 `else if (info.isFile()) { result.push(p) }`）

**問題**: 列挙段階では拡張子を見ずに全ファイルを `result` に積み、`MAX_IMPORT_FILES = 200` で打ち切る。拡張子チェックは後段のループで行い、非対応ファイルは `errors` に落ちるだけ。そのため **.txt / .json / .db などのサイドカーが大量にあるフォルダをドロップすると、名前順で先に並ぶ非画像ファイルが枠を食い潰し、画像がほとんど取り込まれない**（`truncated: true` になるので「上限超過」トーストは出るが、実際には200枚も取り込めていない）。また非画像1件ごとに `errors` が1件増えるので errors も無意味に膨れる。

**修正方針**: `collectImportFiles` の `isFile()` 分岐で `IMPORT_IMAGE_EXTS.has(extname(p).toLowerCase())` のときだけ `result.push(p)` する（ディレクトリ再帰はそのまま）。後段の unsupported チェックは「直接ドロップされた単体ファイル」向けの防衛として残してよいが、フォルダ展開由来の非画像は errors に積まない形になる。`MAX_IMPORT_FILES` の意味が「画像200件」に揃う。

---

## 5.【堅牢性・低〜中】`addTagBulk` に画像存在ガードがなく、1件の FK 違反で一括タグ付け全体がロールバックする

**場所**: `app/src/main/db.ts` の `addTagBulk`（357行目付近）

**問題**: `addTagsBulk`（自動タグ付け用）は「画像が削除済みなら黙って何もしない」ガードをトランザクション内に持つ（320〜331行目のコメント参照）が、**同じ役割の `addTagBulk`（QuickTag / DetailPanel 一括編集の実体）にはガードがない**。ids のうち1件でも削除済み（Undo 猶予明けのコミットと競合した場合など）だと `FOREIGN KEY constraint failed` が throw され、トランザクション全体がロールバック → 有効な画像への付与もすべて失敗し、renderer には汎用エラーだけが返る。

**修正方針**: `addTagsBulk` と同じ存在チェックをループ内に入れる（存在しない id はスキップして続行）:

```ts
export function addTagBulk(imageIds: number[], tagName: string, source: 'manual' | 'ai' = 'manual'): void {
  if (imageIds.length === 0) return
  prepare('INSERT OR IGNORE INTO tags (name) VALUES (?)').run(tagName)
  const tag = prepare('SELECT id FROM tags WHERE name = ?').get(tagName) as { id: number }
  const insertIt = prepare(UPSERT_IMAGE_TAG)
  const imageExists = prepare('SELECT 1 FROM images WHERE id = ?')
  db.transaction(() => {
    for (const id of imageIds) {
      if (!imageExists.get(id)) continue
      insertIt.run(id, tag.id, source)
    }
  })()
}
```

`db.test.ts` に「存在しない id 混在でも残りに付与される」ケースを追加推奨。

---

## 6.【バグ・低】タイトルが空文字 `''` のまま DB に保存される（null にならない）

**場所**: `app/src/main/bootstrap.ts` の `onCaptureDone`（`title: timecode?.title ?? null`）

**問題**: 拡張から届く `title` は `ws-server.ts` の `text()` により「文字列でなければ `''`」に正規化される。DMM TV などで DOM がタイトルを返せないタイミング（`getPageTitle` が意図的に `''` を返すケース）にキャプチャすると、`'' ?? null` は `''` のまま（`??` は null/undefined のみ）なので **`title = ''` の行ができる**。表示は `cleanTitle` が `'—'` に落とすので実害は小さいが、`title IS NULL` を前提にした将来の処理・エクスポート（`sanitizeFilename(image.title || ...)` は偶然 `||` なので無事）との不整合の芽になり、タイムラインでは「タイトルなし」グループではなく `'—'` キーのグループに入る。

**修正方針**: `title: timecode?.title || null` に変更（`current_time`/`url` は既に null 設計なのでそのまま）。同型の箇所は `ipc-share.ts` の `entry.title ... || null` は対応済み、`ipc-import.ts` も `rawName.length > 0` チェック済みで問題なし。

---

## 7.【堅牢性・低】`captureSetHotkey` が `changeHotkey` に onError を渡しておらず、巻き戻し失敗が依然ユーザーに届かない

**場所**: `app/src/main/bootstrap.ts` の `CH.captureSetHotkey` ハンドラ（`const ok = changeHotkey(normalized)`）

**問題**: 前回レビュー 6-5 で `changeHotkey(newHotkey, onError)` に onError 引数が追加されたが、**唯一の呼び出し元が onError を渡していない**ため、「新キー登録失敗 → 旧キー再登録も失敗（＝ホットキー完全喪失）」のケースは今も console.warn 止まり。UI には「競合しています」と旧ホットキーが表示され続けるが、実際にはその旧キーも登録されていない。ウィンドウ focus 時の `reclaimHotkeysIfFree` で自己修復されるため実害は限定的だが、通知経路を用意した意図が呼び出し側で死んでいる。

**修正方針**:

```ts
const ok = changeHotkey(normalized, (m) => sendNotice('error', m))
```

（設定変更はアプリ内操作なのでブラウザ通知ではなく `sendNotice`＝アプリ内トーストが適切）

---

## 8.【堅牢性・低】WS ポート(39821)が他プロセスに使われていると、無通知のまま拡張連携が死ぬ

**場所**: `app/src/main/ws-server.ts` の `startWsServer`（`httpServer.on('error', ...)` は console.error のみ）

**問題**: 二重起動は singleInstanceLock で防いでいるが、**他のアプリが 39821 を LISTEN している場合**は `EADDRINUSE` が console に出るだけでアプリは普通に起動し、キャプチャ時に「キャプチャ対象を検出できませんでした」という別の警告に化ける。原因（ポート占有）へ辿り着く手掛かりがユーザーに一切ない。

**修正方針**: `httpServer.on('error')` で `EADDRINUSE` を判別し、`sendNotice('error', 'ポート39821が他のアプリに使用されているため、ブラウザ拡張と接続できません。')` を送る（`sendNotice` はウィンドウ生成前だと消えるため、bootstrap の `did-finish-load` 後に送る破損設定通知と同じパターンでフラグ渡しにするのが安全）。

---

## 9.【改善・低】`ping` キープアライブが定義だけあって誰も送っておらず、アイドル時に MV3 SW の切断→再接続が約30秒周期で繰り返される

**場所**: `extension/content.js`（送信側が存在しない）、`extension/background.js` の `normalizePortMessage`（`type: 'ping'` 受理）、`app/src/main/ws-server.ts` の `parseExtensionMessage`（同）

**問題**: `ping` メッセージ型は background・ws-server の両方で受理されるのに、**送信するコードがどこにもない**（デッドコード）。その結果、動画が一時停止中かつタブ非フォーカスのときは `sendTimecode` がスキップされ続けて port/WS が無通信になり、Chrome が MV3 Service Worker をアイドル停止 → port 切断 → content.js の `scheduleReconnect`（1秒後）→ SW 再起動 → WS 再接続、というチャーンが視聴を止めている間じゅう繰り返される。機能上は自己修復するが、切断の瞬間にキャプチャすると `pre-capture`/`request-timecode` を取りこぼす窓がある。

**修正方針**: content.js で port 接続中に 20〜25 秒間隔で `port.postMessage({ type: 'ping' })` を送る（`connectPort` 成功時に setInterval、`onDisconnect`/`pagehide` で clear）。background は受理した ping を `sendWsMessage` でそのまま WS へ流せば、SW と WS の両方のアイドルタイマーが更新される。実装しない判断をするなら、両受理コードの `ping` 分岐は削除して意図を明確にする。

---

## 10.【小・低】Viewer のホイールズームで `preventDefault` が passive リスナー内で呼ばれ、console に intervention 警告が出続ける

**場所**: `app/src/renderer/src/components/Viewer.tsx` の `handleWheel`（`onWheel={handleWheel}` 経由）

**問題**: React 17+ は wheel イベントをルートに **passive: true** で登録するため、`handleWheel` 冒頭の `e.preventDefault()` は無効で、ホイール操作のたびに `Unable to preventDefault inside passive event listener invocation.` が console に出る。現在のレイアウトではビューア背後にスクロール連鎖しないため実害は警告のみだが、ズーム操作のたびのログ汚染はデバッグ性を下げる。

**修正方針**: overlay 要素の ref に対して `useEffect` で `el.addEventListener('wheel', handler, { passive: false })` を自前登録し、`onWheel` prop は外す（クリーンアップで remove）。

---

## 11.【デッドコード・低】書き込まれるだけで読まれない状態が2箇所

1. **`filterStore.searchComposing`**（`app/src/renderer/src/stores/filterStore.ts`）: `setSearchComposing` は Toolbar の composition イベントから呼ばれるが、`searchComposing` を読むコードが存在しない（IME ガードは Toolbar の `onChange` 内 `isComposing` チェックで完結している）。state・setter・`useFilters` の配線・Toolbar の `onCompositionStart/End` をまとめて削除してよい。
2. **`DetailPanel.memoSavingRef`**（`app/src/renderer/src/components/DetailPanel.tsx`）: 4箇所で代入されるが読み取りが1つもない。Escape 時の二重保存防止は `pendingMemoSaveRef` クリア＋draft 巻き戻しで既に成立している。削除してよい。

いずれも「ガードが効いているように見えて実は効いていない」誤読を誘うのが害。

---

## 12.【小・低】CSP の `connect-src ws://127.0.0.1:39821` は不要

**場所**: `app/src/renderer/index.html` の CSP メタタグ

**問題**: WS サーバーへ接続するのは Chrome 拡張であって renderer ではない（renderer 内に WebSocket 使用箇所なし）。renderer から localhost ソケットへの接続許可は攻撃面をわずかに広げるだけなので、`connect-src 'self'` に絞ってよい。動作確認: 起動して一覧表示・キャプチャ反映（IPC 経由なので CSP 無関係）が普通に動くこと。

---

## 13.【UX・任意】共有インポート（shareImport）に進捗表示もキャンセルもない

**場所**: `app/src/main/ipc-share.ts` の `shareImport`、`app/src/renderer/src/components/SettingsModal.tsx`

**問題**: エクスポート側は `export:progress` + 中止ボタンを持つのに、インポートは最大1000件のファイルコピー＋DB挿入をボタンが「読み込み中...」になるだけで完走まで待たせる。大きなライブラリでは分単位になり、途中で止める手段がない。

**修正方針（対応するなら）**: `shareExport` と同じパターンを流用する — `isShareImportCanceled` フラグ + `CH.shareImportCancel`、ループ内で `sendToRenderer(CH.exportProgress, ...)`（チャンネル共用で renderer の activeTask 帯にそのまま載る。`exportStore.startExport('share-import')` 相当の kind 追加が要る）。

---

## 14.【性能・任意】`backfillThumbnails` が毎起動、全画像行に対してファイル存在確認を走らせる

**場所**: `app/src/main/ipc-images.ts` の `backfillThumbnails`（bootstrap から毎起動呼ばれる）と `db.ts` の `listImagesForThumbCheck`（全行 SELECT）

**問題**: サムネがあるか否かに関わらず全行を列挙し、各行で `resolveRealCapturePath`（realpath）＋ `access` を実行する。1万枚なら毎起動 2〜3万回のファイルシステム呼び出しになる。バックグラウンド async なので UI は塞がないが、起動直後のディスク負荷（特に HDD / OneDrive 配下）としては無視しにくい。※このプロジェクトの userData は OneDrive 同期外だが、captures フォルダが大きいユーザーほど効く。

**修正方針（対応するなら）**: 2段構えにする — (1) `WHERE thumb_path IS NULL OR thumb_path = ''` の行だけを毎起動チェック（欠損補完の本来目的）、(2) 「thumb_path はあるが実ファイルが消えている」ケースの全件検査は、`migrate-thumbnails.ts` と同様のマーカー（例: アプリバージョンごとに1回）に落とす。

---

## 15.【コメント修正のみ・任意】コメントと実装の不一致 2件

1. `app/src/renderer/src/components/Toolbar.tsx` 299行目付近「Enter は **200ms デバウンス**を待たず即座に確定する」— 現実装の `onChange` は素のキーワードを**毎打鍵で即 commit** しておりデバウンスは存在しない。コメントを実装に合わせる（あるいは実際に 200ms デバウンスを入れて打鍵ごとの `listImages`+`countImages` を減らす。FTS でも 1〜2 文字時は LIKE 全走査なので入れる価値はある）。
2. `app/src/main/bootstrap.ts` 296行目付近「OS コンポジタ向けに **50ms** マージン」— 実装は `setTimeout(..., 20)`。数値をどちらかに揃える。

---

## 確認して問題なしと判断した領域（修正不要・参考）

- **パス検証**: `paths.ts` の `resolveCapturePath`/`resolveRealCapturePath`（realpath + ベースディレクトリ封じ込め + 拡張子ホワイトリスト）は capfile プロトコル・削除・エクスポート・クリップボードの全経路で通っている
- **IPC 信頼境界**: 全ハンドラが `handleTrusted`（sender id + frame URL 検証）経由。`shareImport` のファイル名も `basename` 一致チェックでトラバーサル不可
- **WS**: origin の拡張 ID allowlist、ペイロード上限、全フィールドの範囲検証（ws-server / background の二重検証）は整合
- **SQL**: すべて prepared statement。LIKE エスケープ・FTS フレーズエスケープあり。FTS トリガは INSERT/UPDATE/DELETE を網羅
- **タガー**: モデル DL の SHA-256 ピン・無通信タイムアウト・tmp→rename・推論直列化・アイドル解放と削除のチェーン合流はいずれも正しく組めている
- **削除順序**（DB → ゴミ箱）、Undo 猶予の pagehide フラッシュ、トーストのアクション付き優先残留は前回修正どおり機能する形になっている
