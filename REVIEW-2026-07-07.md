# コードレビュー指摘事項（2026-07-07 / v1.0.4 全体レビュー）

Git 差分ではなくコードベース全体を対象としたレビューの結果。
別セッション（他モデル）が本ファイルだけを読んで修正できるよう、根拠・修正方針を自己完結で記載する。

**ステータス: 1〜6-5 すべて対応済み**（コミット `d40e190`, `1ad3649`）。6-6 のみ既知の限界として据え置き。
色フィルターは方針 c（デッドコード削除）を採用し、`colors` DB カラムのみ残置。

- 検証済み: `cd app && npm run verify` → typecheck エラーなし・231 テスト全パス（色フィルター関連4件を削除したため 235→231）

---

## 1.【バグ・優先度高】拡張: テキスト入力中でも Shift+←/→ がコマ送りを発動する → ✅ 修正済み

**場所**: `extension/content.js` の末尾付近、`document.addEventListener('keydown', ...)`（capture: true のコマ送りハンドラ。`if (!e.shiftKey || (e.key !== 'ArrowLeft' && e.key !== 'ArrowRight')) return` で始まるもの）

**問題**: このハンドラには編集中ターゲットのガードがない。YouTube の検索欄・コメント欄など対応サイトのテキスト入力中に Shift+←/→（テキスト範囲選択の標準操作）を押すと、`preventDefault` + `stopPropagation` されて選択できず、代わりに動画が pause + コマ送りされる。拡張は Shiori アプリ非起動時も対応サイトで常時動くため影響が広い。

**修正**: ハンドラ先頭に編集ターゲットガードを追加する。

```js
document.addEventListener('keydown', (e) => {
  const t = e.target
  if (t && (t.tagName === 'INPUT' || t.tagName === 'TEXTAREA' || t.isContentEditable)) return
  if (!e.shiftKey || (e.key !== 'ArrowLeft' && e.key !== 'ArrowRight')) return
  // ...既存処理...
}, true)
```

---

## 2.【バグ・優先度高】エクスポート: 1枚のコピー失敗で全体が中断する → ✅ 修正済み

**場所A**: `app/src/main/ipc-images.ts` の `CH.imagesExport` ハンドラ内 `copyOne`
**場所B**: `app/src/main/ipc-share.ts` の `CH.shareExport` ハンドラ内 `copyOne`

**問題**: どちらの `copyOne` も `stat` の失敗は catch しているが、本体の `copyFile`（A ではさらに `uniqueExportPath`）が try で囲われていない。1枚でも例外が出ると for ループごと巻き込んで IPC invoke 全体が reject する。

- A: 残りの画像が書き出されず、renderer は一括の「エクスポートに失敗しました」になる
- B: さらに深刻で、ループ後に書くはずの `metadata.jsonl` / `settings.json` が一切書かれず、コピー済み画像だけが残る「インポート不能な部分出力」になる（キャンセル時は部分出力でも読み込める設計なのに、例外時はその保証が破れる）

**現実的な失敗要因**: タイトル由来ファイル名（`sanitizeFilename` は 120 字まで許容）+ 日付 + 深いエクスポート先で Windows の MAX_PATH(260) 超過、AV/同期ソフトの一時ロック、権限エラー等。

**修正**: 両 `copyOne` で `copyFile`（および A の `uniqueExportPath`）を try/catch で包み、失敗時は `console.warn` して A は `false`、B は `null` を返して「その1枚をスキップして続行」にする。B のサムネ側は既に try/catch 済みなので本体側だけ揃えればよい。

---

## 3.【機能不整合・優先度高】色フィルター機能が端から端まで死んでいる → ✅ 対応済み（方針 c）

**事実関係**（すべて確認済み）:

- `images.colors` カラムに非 NULL を書き込むコードがどこにも存在しない。全 insert 経路（`app/src/main/bootstrap.ts` のキャプチャ、`ipc-import.ts` のクリップボード/ファイル取込、`ipc-share.ts` の共有インポート）が `colors: null` を渡し、colors を UPDATE する SQL もない
- `listColors` IPC（`db.ts` の `listColors`、preload の `listColors`）は renderer から一度も呼ばれない。`filterStore.allColors` は常に空で、`setAllColors` に API 結果を流すコードがない
- `setColorFilter` を非 null で呼ぶ UI が存在しない（呼び出しは Toolbar のフィルタチップ「解除」と、色付きスマートフォルダの読み込みのみ。色付きスマートフォルダは現 UI では作成不可能）

つまり色関連コード（DB スキーマ・`buildImageFilter` の color 条件・`colorDist`/`listColors`・IPC・ストア）は一式残っているが機能しない。一方 `README.md` は「タグ/**色**/日付フィルターに対応」と宣伝している。旧バージョンで色抽出があった名残と思われる。

**修正方針（どれか選ぶ。判断がつかなければ a を推奨）**:

- a. **README から「色」の記載を削除**（最小対応・リリース済みデータと非互換にならない）
- b. 色抽出（キャプチャ/インポート時にサムネから代表色を計算して colors に保存）+ サイドバーの色スウォッチ UI を実装する（工数大）
- c. 色関連のデッドコードを削除する（`ImageQuery.color`、`buildImageFilter` の color 条件、`listColors`/`colorDist`、IPC、ストアの colorFilter/allColors、SmartFolder.color）。ただし既存 DB の colors カラム自体は残してよい（SQLite の列削除は不要）。テスト（`db.test.ts` の color 系）も更新が要る

a を選ぶ場合は `README.md` 8行目付近「タグ/色/日付フィルターに対応します」と 17 行目付近「タグ（AND/OR）、色、日付、サービスで絞り込み」から「色」を外すだけ。

---

## 4.【バグ・中】グリッド矩形選択のギャップ定数の食い違い → ✅ 修正済み

**場所**: `app/src/renderer/src/hooks/useSelection.ts` 冒頭の `const GAP = 8` と、それを使う `hitTestBox`

**問題**: 実際のグリッド描画は `app/src/renderer/src/App.tsx` の `COL_GAP = 6` でレイアウトされる（セル幅も `computeGridLayout(containerWidth, thumbnailSize, COL_GAP)` で算出）。しかし矩形選択の当たり判定 `hitTestBox` はセル左端を `col * (cellWidth + GAP)` = 8px ギャップで計算するため、想定位置が列ごとに 2px ずつずれる。列数が多い（サムネ小・ウィンドウ広）とき右端付近で最大数十 px ずれ、矩形選択が取りこぼす/余計に選ぶ。タイムライン側は DOM 実測（`hitTestDomBox`）なので影響なし。

**修正**: App.tsx の `COL_GAP`（と必要なら `ROW_GAP`/`LABEL_HEIGHT`）を共有定数化して useSelection と同じ値を使わせる。最小修正なら `UseSelectionOptions.gridLayout` に `colGap` を追加して App から `COL_GAP` を渡し、`hitTestBox` の `GAP` をそれに置き換える（`hitHeight = Math.max(cellHeight, rowHeight - GAP)` の GAP は行間 = `ROW_GAP` の意図なので、こちらは 10 を渡すか現状維持でも実害は小さい）。

---

## 5.【UX・中】ホットキー競合中、ウィンドウフォーカスのたびにブラウザへエラー通知が出る → ✅ 修正済み

**場所**: `app/src/main/bootstrap.ts` の `reclaimHotkeysIfFree`（`createWindow(reclaimHotkeysIfFree)` として focus イベントに接続）

**問題**: 他アプリがホットキーを取っている間、Shiori ウィンドウを focus するたびに `registerHotkey` が失敗 → `onError` → `sendBrowserNotice('error', 'ホットキー ... を登録できませんでした...')` が拡張へ飛び、視聴中ページにエラーオーバーレイが毎回表示される。Shiori ⇔ ブラウザを行き来するだけでスパムになる。

**修正**: `reclaimHotkeysIfFree` に「前回も失敗していたら通知を抑止する」フラグを持たせる。例:

```ts
let reclaimNotifiedFailure = false
function reclaimHotkeysIfFree(): void {
  const st = loadSettings()
  if (globalShortcut.isRegistered(st.captureHotkey)) return
  const ok = registerHotkey(st.captureHotkey, (m) => {
    if (!reclaimNotifiedFailure) sendBrowserNotice('error', m)
  })
  reclaimNotifiedFailure = !ok
}
```

（成功したらフラグを戻す。起動時の初回 `registerHotkey` の通知は現状のままでよい）

---

## 6.【軽微・任意】その他

### 6-1. 共有インポートの `captured_at` が範囲未検証 → ✅ 修正済み
`app/src/main/ipc-share.ts` の shareImport で `entry.captured_at` は `Number.isFinite` のみ。`1e300` 等が来ると `ensureCaptureSubDir` が `NaN-NaN` フォルダを作り、DB の並び順も恒久的に壊れる。妥当な epoch 範囲（例: `0 < v && v < 4102444800000` /2100年/、範囲外は `Date.now()` にフォールバック）へクランプする。

### 6-2. Undo 猶予中にアプリ終了すると削除が実行されない → ✅ 修正済み
削除は 4 秒の Undo 猶予後に DB/ファイルへ反映される（`useSelection.ts` の `queueDelete` → `commitPendingDelete`）。猶予中にトレイから「終了」するとレンダラーごと消えて削除が確定せず、次回起動時に画像が復活する（トーストは「移動しました」と言った後なので不整合）。対応するなら: main 側に「終了前フラッシュ」用 IPC を設けるのは大掛かりなので、renderer の `beforeunload`/`pagehide` で `pendingDeleteRef` を同期的に commit する（`window.api.deleteImage` の投げっぱなし fire-and-forget でも DB 削除自体は main 側で完走する）。

### 6-3. トースト 3 件上限で「元に戻す」トーストが押し出されうる → ✅ 修正済み
`useToast.ts` の `MAX_TOASTS = 3` 超過時、最古から落とす際にアクション付き（Undo）トーストも落ちる。Undo ボタンだけ早く消えても削除は 4 秒後に確定する。押し出し対象の選定で `action !== null` のトーストをスキップする（アクション付きだけで 3 件を超えるケースは従来どおり最古から落とす）。

### 6-4. `settingsSet` 経由の `captureHotkey` は保存されるが再登録されない → ✅ 修正済み
`bootstrap.ts` の `CH.settingsSet` は `Partial<Settings>` をマージ保存するだけで、`captureHotkey` が含まれていても `changeHotkey` を呼ばない。現 UI は専用 IPC（`CH.captureSetHotkey`）を使うので実害はないが、汎用口の罠。`settingsSet` ハンドラで patch から `captureHotkey` キーを削除（無視）するのが安全:

```ts
const { captureHotkey: _ignored, ...safePatch } = (patch && typeof patch === 'object' ? patch : {}) as Record<string, unknown>
```

### 6-5. `changeHotkey` の巻き戻し失敗が黙殺される → ✅ 修正済み
`app/src/main/capture.ts` の `changeHotkey`: 新キー登録失敗 → 旧キー再登録も失敗した場合、ホットキーが完全に失われても console 警告のみ。少なくとも `registerHotkey(previousHotkey, onError)` と onError を引き回す。

### 6-6. マルチモニタ混在 DPI の既知の限界（コード変更不要・メモのみ）
`capture.ts` の `resolveDisplay` は screenshot-desktop の座標（物理 px 系）と `window.screen*` 由来の CSS px を同一座標系として中心点マッチングしており、スケーリングが異なるモニタ構成では別ディスプレイを掴む余地がある。実機で問題が出ていなければ据え置きでよいが、`resolveDisplay` 付近のコメントに既知の限界として一言残す価値がある。

---

## 修正後の検証

1. `cd app && npm run verify`（typecheck + vitest 235 件）
2. 指摘 1 は可能なら実機で: YouTube の検索欄にテキストを入れ Shift+←/→ で選択できること・動画上では従来どおりコマ送りが効くこと
3. 指摘 4 はテスト追加推奨: `hitTestBox` にギャップを引数として渡す形にすれば単体テスト可能
